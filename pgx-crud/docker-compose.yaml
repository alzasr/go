services:
  pg-crud-pg:
    image: postgres
    restart: always
    container_name: pg-crud-pg
    environment:
      - "POSTGRES_USER=postgres"
      - "POSTGRES_PASSWORD=postgres"
      - "POSTGRES_DB=postgres"
    ports:
      - "5432:5432"
 